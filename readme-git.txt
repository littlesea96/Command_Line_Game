Exclude .idea and .iml because they are created by IntelliJ.
They are configuration files of IntelliJ which not needed for others to clone

Exclude target folder which include all jar files and .class files.
Both are not needed, when the project recompile , those file will be created anyway

Exclude .DS_Store which is created by MacOs.
It is a file that stores custom attributes of its containing folder.
